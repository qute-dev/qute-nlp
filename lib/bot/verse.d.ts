import { Message } from '../models';
export declare function getVerseRange(chapterNo: number, verseStart: number, verseEnd: number): Message;
export declare function getVersesByIds(verseIds: number[]): Message;
